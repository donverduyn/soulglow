// this is an implementation of a mapper with effect-ts that resembles the api of rxjs operators switchMap, mergeMap, concatMap and exhaustMap

// in order to implement switchMap with effect, we want to 
// const switchMap = (effect) => effect;