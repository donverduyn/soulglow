import { describe, expect, it } from 'vitest';

describe('throttle', () => {
  it('dummy test', () => {
    expect.hasAssertions();
    // Arrange
    const expected = true;

    // Act
    const actual = true;

    // Assert
    expect(actual).toBe(expected);
  });
});
