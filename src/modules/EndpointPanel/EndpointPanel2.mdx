import { Meta } from '@storybook/addon-docs';
import EndpointPanel from './EndpointPanel';

<Meta
  title='@EndpointPanel/EndpointPanel2'
  component={EndpointPanel}
/>

# EndpointPanel

Displays a list of endpoints and allows adding new ones.  
Handles runtime behavior and provides a publish function.

## Usage

```jsx
<EndpointPanel />
```
